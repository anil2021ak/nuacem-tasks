import { SkillCardSearchPipe } from './skill-card-search.pipe';

describe('SkillCardSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new SkillCardSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
